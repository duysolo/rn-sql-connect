specVersion: "v1"
serviceId: "example"
location: "asia-southeast1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "example"
      cloudSql:
        instanceId: "example"
connectorDirs: ["./connector"]
